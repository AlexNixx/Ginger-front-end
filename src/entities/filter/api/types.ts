export interface Category {
	name: string
	photoUrl: string
	_id: string
}

export interface Brand {
	name: string
	photoUrl: string
	_id: string
}

export interface Color {
	name: string
	rgb: string
	_id: string
}
