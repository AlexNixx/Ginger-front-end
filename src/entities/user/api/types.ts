export type UpdatePasswordFormValues = {
	password: string
	newPassword: string
	confirmNewPassword: string
}

export type ShippingAddressFormValues = {
	city: string
	address: string
	postalCode: string
	country: string
}
